# Datapack Schema Generation Skill

The reusable AI instructions now live in the project Skill:

- [skills/generate-datapack-schema/SKILL.md](skills/generate-datapack-schema/SKILL.md)

Use `$generate-datapack-schema` in an environment that loads project Skills, or provide that `SKILL.md` to another AI before asking it to generate a Schema. This file remains only as a stable compatibility link.

The current contract also supports localized `$title` and `$description` maps; follow the detailed rules in `skills/generate-datapack-schema/SKILL.md`.
